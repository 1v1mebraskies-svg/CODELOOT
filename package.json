{
  "name": "codeloot-admin",
  "version": "1.0.0",
  "description": "CodeLoot Admin Dashboard - Standalone Vercel Deployment",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site - no build needed'",
    "start": "vercel dev",
    "sync:start": "node scripts/auto-sync.js",
    "sync:stop": "pkill -f 'node scripts/auto-sync.js'"
  },
  "dependencies": {
    "@vercel/blob": "^2.4.0",
    "chokidar": "^3.5.3"
  }
}
